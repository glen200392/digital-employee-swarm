# LLM SDKs（設定 API Key 後啟用）
anthropic>=0.34.0
openai>=1.40.0
google-generativeai>=0.8.0

# Vector Database (in-memory 模式，無需 Qdrant Server)
qdrant-client>=1.10.0

# Web Dashboard
fastapi>=0.115.0
uvicorn>=0.30.0

# Testing
pytest>=8.0.0
httpx>=0.27.0

# Persistent Storage
# sqlite3 is built-in Python, no extra dependency needed
# Optional: for remote Qdrant
# qdrant-client[fastembed]>=1.10.0
